import { HideDivDirective } from './hide-div.directive';

describe('HideDivDirective', () => {
  it('should create an instance', () => {
    const directive = new HideDivDirective();
    expect(directive).toBeTruthy();
  });
});
