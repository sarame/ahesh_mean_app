import { ShowDivDirective } from './show-div.directive';

describe('ShowDivDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDivDirective();
    expect(directive).toBeTruthy();
  });
});
